<?php

class PushesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('pushes')->truncate();

		$pushes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('pushes')->insert($pushes);
	}

}
